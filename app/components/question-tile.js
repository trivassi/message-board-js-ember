import Ember from 'ember';

export default Ember.Component.extend({
  // heading: Ember.computed('question.content','question.author', function(){
  //   return this.get('question.content') + ' - ' + this.get('question.author');
  // })

});
